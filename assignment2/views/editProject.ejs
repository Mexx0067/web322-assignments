<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Project - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 p-8 rounded-lg mb-8">
            <div class="hero-content text-center">
                <div>
                    <h1 class="text-4xl font-bold">Edit Project</h1>
                    <p class="py-6">Update climate solution project details</p>
                </div>
            </div>
        </div>
        
        <div class="max-w-2xl mx-auto">
            <form method="POST" action="/solutions/editProject/<%= project.id %>" class="space-y-4">
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Title</span>
                    </label>
                    <input type="text" name="title" class="input input-bordered w-full" value="<%= project.title %>" required>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Feature Image URL</span>
                    </label>
                    <input type="url" name="feature_img_url" class="input input-bordered w-full" value="<%= project.feature_img_url %>" required>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Sector</span>
                    </label>
                    <select name="sector_id" class="select select-bordered w-full" required>
                        <option value="">Select a sector</option>
                        <option value="1" <%= project.sector_id == 1 ? 'selected' : '' %>>Land Sinks</option>
                        <option value="2" <%= project.sector_id == 2 ? 'selected' : '' %>>Industry</option>
                        <option value="3" <%= project.sector_id == 3 ? 'selected' : '' %>>Transportation</option>
                        <option value="4" <%= project.sector_id == 4 ? 'selected' : '' %>>Electricity</option>
                        <option value="5" <%= project.sector_id == 5 ? 'selected' : '' %>>Food, Agriculture, and Land Use</option>
                    </select>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Short Summary</span>
                    </label>
                    <textarea name="summary_short" class="textarea textarea-bordered w-full" rows="3" required><%= project.summary_short %></textarea>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Introduction</span>
                    </label>
                    <textarea name="intro_short" class="textarea textarea-bordered w-full" rows="4" required><%= project.intro_short %></textarea>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Impact</span>
                    </label>
                    <textarea name="impact" class="textarea textarea-bordered w-full" rows="4" required><%= project.impact %></textarea>
                </div>
                
                <div class="form-control">
                    <label class="label">
                        <span class="label-text">Original Source URL</span>
                    </label>
                    <input type="url" name="original_source_url" class="input input-bordered w-full" value="<%= project.original_source_url %>" required>
                </div>
                
                <div class="flex gap-4">
                    <button type="submit" class="btn btn-success flex-1">Update Project</button>
                    <a href="/solutions/projects" class="btn btn-secondary flex-1">Cancel</a>
                </div>
            </form>
            
            <div class="divider">Danger Zone</div>
            
            <form method="POST" action="/solutions/deleteProject/<%= project.id %>" onsubmit="return confirm('Are you sure you want to delete this project? This action cannot be undone.');">
                <button type="submit" class="btn btn-error w-full">Delete Project</button>
            </form>
        </div>
    </div>
</body>
</html>