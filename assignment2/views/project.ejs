<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.title %> - Climate Solutions</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 p-8 rounded-lg">
            <div class="hero-content text-center">
                <div>
                    <h1 class="text-4xl font-bold"><%= project.title %></h1>
                    <p class="py-6">Below, you will find detailed information about the project: <%= project.title %></p>
                </div>
            </div>
        </div>
        <div class="mt-8">
            <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="w-full h-64 object-cover rounded-lg mb-4">
            <h2 class="text-2xl font-bold">Introduction</h2>
            <p class="prose"><%= project.intro_short %></p>
            <h2 class="text-2xl font-bold mt-4">Impact</h2>
            <p class="prose"><%= project.impact %></p>
            <a href="<%= project.original_source_url %>" class="btn btn-primary mt-4" target="_blank">Learn More</a>
            <div class="mt-4">
                <h2 class="text-2xl font-bold">Quote</h2>
                <p id="quote" class="prose italic"></p>
                <p id="author" class="prose font-bold"></p>
            </div>
            <a href="#" onclick="history.back(); return false;" class="btn btn-secondary mt-4">Back</a>
        </div>
    </div>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch('https://dummyjson.com/quotes/random')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('quote').textContent = `"${data.quote}"`;
                    document.getElementById('author').textContent = `- ${data.author}`;
                })
                .catch(error => console.error('Error fetching quote:', error));
        });
    </script>
</body>
</html>